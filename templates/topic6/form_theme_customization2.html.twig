{% extends "topic6/base.html.twig" %}

{% form_theme form _self %}

{% block _another_example_form_country_widget %}
    <div {{ block('widget_container_attributes') }}>
        {% for child in form %}
        <div class="every-choice-in-its-own-div">
            {{ form_widget(child) }}
            {{ form_label(child, null, {translation_domain: choice_translation_domain}) }}
        </div>
        {% endfor %}
    </div>
{% endblock %}

{% block content %}
    {{ form(form) }}
{% endblock %}